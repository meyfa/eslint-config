/**
 * Example interface.
 */
export interface Example {
  /**
   * Maps the provided value.
   *
   * @param {number} value The input value.
   * @returns {number} The mapped value.
   */
  map: (value: number) => number
}
